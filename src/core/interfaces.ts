export interface IPlannerTask {
    id: string,
    title: string,
    completed: boolean,
    importance?: string;
}

export interface IPlannerModel {
    // key: any;
    // todos: Array<IPlannerTask>;
    // onChanges: Array<any>;
    // subscribe(onChange);
    // inform();
    // addTodo(title: string);
    // toggleAll(checked);
    // toggle(todoToToggle);
    // destroy(todo);
    // save(todoToSave, text);
    // clearCompleted();
}